// @flow
import { StyleSheet } from 'aphrodisiac';

const trend = {
  backgroundColor: 'purple',
  position: 'absolute',
  bottom: '0',
};

const styles: {
  trend: StyleSheetProp,
} = StyleSheet.create({
  trend,
});

export default styles;